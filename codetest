local Players = game:GetService("Players")
local RS      = game:GetService("ReplicatedStorage")
local Net     = RS:WaitForChild("Packages"):WaitForChild("Net")
local PG      = Players.LocalPlayer:WaitForChild("PlayerGui")

if typeof(firesignal) ~= "function" then
    warn("[Krimo Hub] executor has no 'firesignal' - cannot fake announcements.")
    return
end

-- resolve the notify remote: Phi's _G, else via NotificationController
local function resolveRemote()
    if _G.PhiNotifyRemote then return _G.PhiNotifyRemote end
    local getinfo = debug and (debug.getinfo or debug.info)
    if getgc and getinfo then
        if getconnections then
            for _, d in ipairs(Net:GetDescendants()) do
                if d:IsA("RemoteEvent") then
                    local ok, cs = pcall(getconnections, d.OnClientEvent)
                    if ok then
                        for _, c in ipairs(cs) do
                            local f, fn = pcall(function() return c.Function end)
                            if f and type(fn) == "function" then
                                local i, info = pcall(getinfo, fn)
                                if i and tostring(info.short_src or info.source or ""):find("NotificationController", 1, true) then
                                    return d
                                end
                            end
                        end
                    end
                end
            end
        end
    end
end

local remote = resolveRemote()
if not remote then
    warn("[Krimo Hub] notify remote not found - load Phi first, then run this.")
    return
end
print("[Krimo Hub] using remote:", remote.Name)

local function fire(txt)
    txt = (txt or ""):gsub("^%s+", ""):gsub("%s+$", "")
    if txt == "" then return end
    firesignal(remote.OnClientEvent, txt, 5.5, "Sounds.Sfx.Blop", "Top", 2678001507)
end

-- minimal UI (Krimo Hub Theme: Dunkelblau & Schwarz)
local old = PG:FindFirstChild("KrimoHubSender"); if old then old:Destroy() end
local SG = Instance.new("ScreenGui"); SG.Name = "KrimoHubSender"
SG.ResetOnSpawn = false; SG.DisplayOrder = 205; SG.Parent = PG

local function corner(r, p)
    local c = Instance.new("UICorner", p)
    c.CornerRadius = UDim.new(0, r)
end

local function stroke(p)
    local s = Instance.new("UIStroke", p)
    s.Color = Color3.fromRGB(28, 55, 90) -- Dunkelblauer Accent-Rand
    s.Transparency = 0.2
    return s
end

local W = Instance.new("Frame", SG)
W.Size = UDim2.new(0, 220, 0, 125) -- Leicht vergrößert für den Link-Button unten rechts
W.Position = UDim2.new(1, -240, 0.5, -62)
W.BackgroundColor3 = Color3.fromRGB(10, 14, 23) -- Sehr dunkles Blau / Schwarz
W.BackgroundTransparency = 0.05
W.BorderSizePixel = 0
corner(10, W)
stroke(W)

local head = Instance.new("Frame", W)
head.Size = UDim2.new(1, 0, 0, 24)
head.BackgroundColor3 = Color3.fromRGB(15, 23, 42) -- Kopfzeile Dunkelblau
head.BorderSizePixel = 0
corner(10, head)

local title = Instance.new("TextLabel", head)
title.BackgroundTransparency = 1
title.Size = UDim2.new(1, -32, 1, 0)
title.Position = UDim2.new(0, 10, 0, 0)
title.Text = "KRIMO HUB SENDER" -- Name geändert
title.TextColor3 = Color3.fromRGB(220, 235, 255)
title.Font = Enum.Font.GothamBold
title.TextSize = 10
title.TextXAlignment = Enum.TextXAlignment.Left

local close = Instance.new("TextButton", head)
close.Size = UDim2.new(0, 16, 0, 16)
close.Position = UDim2.new(1, -22, 0.5, -8)
close.BackgroundColor3 = Color3.fromRGB(30, 41, 59)
close.Text = "X"
close.TextColor3 = Color3.fromRGB(200, 215, 235)
close.Font = Enum.Font.GothamBold
close.TextSize = 9
close.BorderSizePixel = 0
corner(6, close)
close.MouseButton1Click:Connect(function() SG:Destroy() end)

-- Drag mechanics
do
    local drag, ds, sp
    head.InputBegan:Connect(function(i)
        if i.UserInputType == Enum.UserInputType.MouseButton1 then
            drag = true
            ds = i.Position
            sp = W.Position
            i.Changed:Connect(function()
                if i.UserInputState == Enum.UserInputState.End then drag = false end
            end)
        end
    end)
    game:GetService("UserInputService").InputChanged:Connect(function(i)
        if drag and i.UserInputType == Enum.UserInputType.MouseMovement then
            local d = i.Position - ds
            W.Position = UDim2.new(sp.X.Scale, sp.X.Offset + d.X, sp.Y.Scale, sp.Y.Offset + d.Y)
        end
    end)
end

local box = Instance.new("TextBox", W)
box.Size = UDim2.new(1, -16, 0, 24)
box.Position = UDim2.new(0, 8, 0, 32)
box.BackgroundColor3 = Color3.fromRGB(6, 9, 15) -- Tiefschwarz
box.BorderSizePixel = 0
box.PlaceholderText = "message..."
box.PlaceholderColor3 = Color3.fromRGB(80, 100, 130)
box.Text = ""
box.TextColor3 = Color3.fromRGB(230, 240, 255)
box.ClearTextOnFocus = false
box.Font = Enum.Font.GothamBold
box.TextSize = 11
box.TextXAlignment = Enum.TextXAlignment.Left
corner(5, box)
stroke(box)

local pad = Instance.new("UIPadding", box)
pad.PaddingLeft = UDim.new(0, 6)
pad.PaddingRight = UDim.new(0, 6)

local send = Instance.new("TextButton", W)
send.Size = UDim2.new(0, 110, 0, 24)
send.Position = UDim2.new(0, 8, 0, 62)
send.BackgroundColor3 = Color3.fromRGB(30, 58, 138) -- Dunkelblau
send.Text = "SEND"
send.TextColor3 = Color3.fromRGB(240, 245, 255)
send.Font = Enum.Font.GothamBold
send.TextSize = 11
send.BorderSizePixel = 0
send.AutoButtonColor = true
corner(5, send)

local tc = Instance.new("TextButton", W)
tc.Size = UDim2.new(0, 80, 0, 24)
tc.Position = UDim2.new(0, 126, 0, 62)
tc.BackgroundColor3 = Color3.fromRGB(15, 23, 42)
tc.Text = "CODE"
tc.TextColor3 = Color3.fromRGB(200, 220, 255)
tc.Font = Enum.Font.GothamBold
tc.TextSize = 9
tc.BorderSizePixel = 0
tc.AutoButtonColor = true
corner(5, tc)
stroke(tc)

-- Discord-Link unten rechts
local discordBtn = Instance.new("TextButton", W)
discordBtn.Size = UDim2.new(1, -16, 0, 18)
discordBtn.Position = UDim2.new(0, 8, 1, -24)
discordBtn.BackgroundTransparency = 1
discordBtn.Text = "Discord: discord.gg/tv5r7YdPUZ"
discordBtn.TextColor3 = Color3.fromRGB(96, 165, 250)
discordBtn.Font = Enum.Font.Gotham
discordBtn.TextSize = 8
discordBtn.TextXAlignment = Enum.TextXAlignment.Right

discordBtn.MouseButton1Click:Connect(function()
    if setclipboard then
        setclipboard("https://discord.gg/tv5r7YdPUZ")
        discordBtn.Text = "Link in Zwischenablage kopiert!"
        task.wait(2)
        discordBtn.Text = "Discord: discord.gg/tv5r7YdPUZ"
    end
end)

send.MouseButton1Click:Connect(function() fire(box.Text); box.Text = ""; box:CaptureFocus() end)
box.FocusLost:Connect(function(enter) if enter then fire(box.Text); box.Text = ""; box:CaptureFocus() end end)
tc.MouseButton1Click:Connect(function() fire("CODE" .. tostring(math.random(1000, 9999))) end)

print("[Krimo Hub] ready - load Phi to catch what you send.")
